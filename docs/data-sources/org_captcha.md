---
page_title: "Data Source: okta_org_captcha"
description: |-
  Retrieves the CAPTCHA settings object for your organization. > Note: If the current organization hasn't configured CAPTCHA Settings, the request returns an empty object.
---

# Data Source: okta_org_captcha

Retrieves the CAPTCHA settings object for your organization. > **Note**: If the current organization hasn't configured CAPTCHA Settings, the request returns an empty object.

## Example Usage

```terraform
resource "okta_captcha" "test" {
  name       = "testAcc_replace_with_uuid"
  type       = "HCAPTCHA"
  site_key   = "random_key"
  secret_key = "random_secret_key"
}

resource "okta_org_captcha" "test" {
  captcha_id    = okta_captcha.test.id
  enabled_pages = ["SIGN_IN"]
}

data "okta_org_captcha" "test" {
  depends_on = [okta_org_captcha.test]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (String) Unique identifier of the org_captcha.

### Read-Only

- `captcha_id` (String) The unique key of the associated CAPTCHA instance
- `enabled_pages` (List of String) An array of pages that have CAPTCHA enabled
