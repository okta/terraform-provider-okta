---
page_title: "Data Source: okta_app_sign_on_policy_rule"
description: |-
  Retrieves an app sign-on (access) policy rule by ID from Okta.
---

# Data Source: okta_app_sign_on_policy_rule

Retrieves an app sign-on (access) policy rule by `ruleId` from Okta.

~> **WARNING:** This feature is only available as a part of the 
Identity Engine. [Contact support](mailto:dev-inquiries@okta.com) 
for further information.

-> Inside the product a sign-on policy rule is referenced as an _authentication
policy rule_, in the public API the parent policy is of type
[`ACCESS_POLICY`](https://developer.okta.com/docs/reference/api/policy/#policy-object).

## Example Usage

```terraform
data "okta_app_signon_policy" "example" {
  app_id = "<app id>"
}

data "okta_app_sign_on_policy_rule" "example" {
  id        = "<rule id>"
  policy_id = data.okta_app_signon_policy.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) Unique identifier of the access policy rule.
- `policy_id` (String) `id` of the policy

### Read-Only

- `created` (String) Timestamp when the rule was created
- `last_updated` (String) Timestamp when the rule was last modified
- `name` (String) Name of the rule
- `priority` (Number) Priority of the rule
- `status` (String) Whether or not the rule is active.
- `system` (Boolean) Specifies whether Okta created the policy rule (`system=true`).

### Blocks

#### `actions`

- `app_sign_on` (Block) Specifies the results when a user attempts to sign in
  - `access` (String) Access value (`ALLOW` or `DENY`)
  - `keep_me_signed_in` (Block) Controls how often the post-authentication prompt is presented to users
    - `post_auth` (String) Whether the post-authentication Keep Me Signed In (KMSI) prompt is enabled
    - `post_auth_prompt_frequency` (String) A time duration (ISO 8601) for how often the KMSI prompt appears
  - `verification_method` (Block) The method used to verify a user
    - `type` (String) Verification method type

#### `conditions`

- `device` (Block) Specifies the device condition to match on
  - `managed` (Boolean) Indicates if the device is managed.
  - `registered` (Boolean) Indicates if the device is registered.
  - `assurance` (Block) Specifies device assurance policies
    - `include` (List of String) Device assurance policy IDs to include
- `el_condition` (Block) Specifies an Okta Expression Language condition
  - `condition` (String) Expression to match
- `network` (Block) Specifies a network selection mode and zones
  - `connection` (String) Network selection mode
  - `exclude` (List of String) Network zones to exclude
  - `include` (List of String) Network zones to include
- `people` (Block) Specifies users and groups included or excluded by the rule
  - `groups` (Block) Groups condition
    - `exclude` (List of String) Groups to be excluded
    - `include` (List of String) Groups to be included
  - `users` (Block) Users condition
    - `exclude` (List of String) Users to be excluded
    - `include` (List of String) Users to be included
- `platform` (Block) Specifies a particular platform or device to match on
  - `exclude` (List of Block) Platforms to exclude
    - `type` (String) The type of platform
    - `os` (Block) OS
      - `expression` (String) OS expression
      - `type` (String) The type of operating system
      - `version` (Block) OS version condition
        - `match_type` (String) Version match type
        - `value` (String) Version value
  - `include` (List of Block) Platforms to include
    - `type` (String) The type of platform
    - `os` (Block) OS
      - `expression` (String) OS expression
      - `type` (String) The type of operating system
      - `version` (Block) OS version condition
        - `match_type` (String) Version match type
        - `value` (String) Version value
- `risk_score` (Block) Specifies a risk score condition
  - `level` (String) Risk level
  - `min_risk_level` (String) Minimum risk level
- `user_type` (Block) Specifies user types to include or exclude
  - `exclude` (List of String) User types to exclude
  - `include` (List of String) User types to include
