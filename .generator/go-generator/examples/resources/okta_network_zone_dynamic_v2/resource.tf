resource "okta_network_zone_dynamic_v2" "example" {
  type = "<type>"
  name = "Example Name"

  # Optional fields
  # exclude = "<exclude>"
  # include = "<include>"
  # exclude = "<exclude>"
  # include = "<include>"
  # exclude = "<exclude>"
}
