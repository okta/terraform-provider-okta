// Code generated by tf-generator. DO NOT EDIT.
package fwprovider

import (
	"context"
	"fmt"

	"github.com/hashicorp/terraform-plugin-framework/attr"
	"github.com/hashicorp/terraform-plugin-framework/datasource"
	"github.com/hashicorp/terraform-plugin-framework/datasource/schema"
	"github.com/hashicorp/terraform-plugin-framework/diag"
	"github.com/hashicorp/terraform-plugin-framework/types"

	"github.com/okta/terraform-provider-okta/okta/config"
)

var (
	_ datasource.DataSource              = &{{.Name}}DataSource{}
	_ datasource.DataSourceWithConfigure = &{{.Name}}DataSource{}
)

// {{.TitleName}}DataSource defines the data source implementation.
type {{.Name}}DataSource struct {
	Config *config.Config
}

// {{.TitleName}}DataSourceModel describes the data source data model.
type {{.Name}}DataSourceModel struct {
	ID types.String `tfsdk:"id"`
{{- range .ParentParams}}
	{{.GoField}} types.String `tfsdk:"{{.TFAttr}}"`
{{- end}}
{{- range .Properties}}
	{{.GoField}} {{.GoType}} `tfsdk:"{{.TFAttr}}"`
{{- end}}
}
{{range .Properties}}{{if .NestedModelName}}
// {{.NestedModelName}} is the nested model for {{.TFAttr}}.
type {{.NestedModelName}} struct {
{{- range .NestedProps}}
	{{.GoField}} {{.GoType}} `tfsdk:"{{.TFAttr}}"`
{{- end}}
}
{{.NestedStructDefs}}{{end}}{{end}}

func New{{.TitleName}}DataSource() datasource.DataSource {
	return &{{.Name}}DataSource{}
}

func (d *{{.Name}}DataSource) Metadata(_ context.Context, req datasource.MetadataRequest, resp *datasource.MetadataResponse) {
	resp.TypeName = req.ProviderTypeName + "_{{.TFName}}"
}

func (d *{{.Name}}DataSource) Configure(_ context.Context, req datasource.ConfigureRequest, resp *datasource.ConfigureResponse) {
	if req.ProviderData == nil {
		return
	}
	cfg, ok := req.ProviderData.(*config.Config)
	if !ok {
		resp.Diagnostics.AddError(
			"Unexpected DataSource Configure Type",
			"Expected *config.Config, got something else. Please report this issue to the provider developers.",
		)
		return
	}
	d.Config = cfg
}

func (d *{{.Name}}DataSource) Schema(_ context.Context, _ datasource.SchemaRequest, resp *datasource.SchemaResponse) {
	resp.Schema = schema.Schema{
		MarkdownDescription: "Data source for the Okta `{{.TFName}}` resource.",
		Attributes: map[string]schema.Attribute{
			"id": schema.StringAttribute{
				MarkdownDescription: "Unique identifier of the {{.TFName}}.",
				Optional:            true,
				Computed:            true,
			},
{{- range .ParentParams}}
			"{{.TFAttr}}": schema.StringAttribute{
				MarkdownDescription: "{{.Description}}",
				Required:            true,
			},
{{- end}}
{{- range .Properties}}
			"{{.TFAttr}}": {{if .SchemaAttrBlock}}{{.SchemaAttrBlock}},
{{- else}}{{.TFSchemaType}}{
				MarkdownDescription: "{{.Description}}",
{{- if .ElementType}}
				ElementType: {{.ElementType}},
{{- end}}
				Computed: true,
			},
{{- end}}
{{- end}}
		},
	}
}

func (d *{{.Name}}DataSource) Read(ctx context.Context, req datasource.ReadRequest, resp *datasource.ReadResponse) {
	var state {{.Name}}DataSourceModel

	resp.Diagnostics.Append(req.Config.Get(ctx, &state)...)
	if resp.Diagnostics.HasError() {
		return
	}

	if !state.ID.IsNull() && !state.ID.IsUnknown() && state.ID.ValueString() != "" {
		// Fetch by ID using singular endpoint
{{- range .ParentParams}}
		{{.TFAttr}} := state.{{.GoField}}.ValueString()
{{- end}}
		// TODO: Replace with the actual API call
		// client := d.Config.OktaIDaaSClient.OktaSDKClientV6()
{{- if .HasParent}}
		// result, _, err := client.{{.APITag}}API.{{.ReadMethod}}(ctx, {{range .ParentParams}}{{.TFAttr}}, {{end}}state.ID.ValueString()).Execute()
{{- else}}
		// result, _, err := client.{{.APITag}}API.{{.ReadMethod}}(ctx, state.ID.ValueString()).Execute()
{{- end}}
		// if err != nil {
		//     resp.Diagnostics.AddError(
		//         "Failed to read {{.TFName}}",
		//         fmt.Sprintf("Error reading {{.TFName}}: %s", err.Error()),
		//     )
		//     return
		// }
		// map{{.TitleName}}ResponseToState(ctx, result, &state, &resp.Diagnostics)
{{- range .ParentParams}}
		_ = {{.TFAttr}}
{{- end}}
		_ = ctx
	} else {
		// Fetch using list/plural endpoint and filter
{{- range .ParentParams}}
		{{.TFAttr}} := state.{{.GoField}}.ValueString()
{{- end}}
		// TODO: Replace with the actual API call
		// client := d.Config.OktaIDaaSClient.OktaSDKClientV6()
{{- if .HasParent}}
		// results, _, err := client.{{.APITag}}API.{{.ListMethod}}(ctx, {{range .ParentParams}}{{.TFAttr}}, {{end}}).Execute()
{{- else}}
		// results, _, err := client.{{.APITag}}API.{{.ListMethod}}(ctx).Execute()
{{- end}}
		// if err != nil {
		//     resp.Diagnostics.AddError(
		//         "Failed to list {{.TFName}}",
		//         fmt.Sprintf("Error listing {{.TFName}}: %s", err.Error()),
		//     )
		//     return
		// }
		// TODO: Filter results based on state fields and call map{{.TitleName}}ResponseToState
{{- range .ParentParams}}
		_ = {{.TFAttr}}
{{- end}}
		_ = ctx
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &state)...)
}

// map{{.TitleName}}ResponseToState maps the API response to the data source state model.
// TODO: Replace the interface{} response type with the actual SDK response struct.
func map{{.TitleName}}ResponseToState(ctx context.Context, response interface{}, state *{{.Name}}DataSourceModel, diags *diag.Diagnostics) {
	// TODO: Map response fields to state model.
	// Example:
	// state.ID = types.StringValue(response.GetId())
{{- range .Properties}}
	// state.{{.GoField}} = types.StringValue(response.Get{{.GoField}}())
{{- end}}
	_ = ctx
	_ = response
	_ = diags
}

// Ensure fmt is used.
var _ = fmt.Sprintf
