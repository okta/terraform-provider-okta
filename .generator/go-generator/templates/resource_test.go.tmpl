// Code generated by tf-generator. DO NOT EDIT.
package fwprovider

import (
	"context"
	"fmt"
	"testing"

    "github.com/okta/terraform-provider-okta/okta/acctest"
	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
	"github.com/hashicorp/terraform-plugin-testing/terraform"
)

func TestAccOkta{{.TitleName}}_basic(t *testing.T) {
	resourceName := "okta_{{.TFName}}.test"

	resource.Test(t, resource.TestCase{
		PreCheck:                 acctest.AccPreCheck(t),
		ProtoV5ProviderFactories: acctest.ProtoV5ProviderFactoriesForTestAcc(t),
		CheckDestroy:             testAccCheckOkta{{.TitleName}}Destroy,
		Steps: []resource.TestStep{
			{
				Config: testAccOkta{{.TitleName}}Config_basic(),
				Check: resource.ComposeTestCheckFunc(
					testAccCheckOkta{{.TitleName}}Exists(resourceName),
					resource.TestCheckResourceAttrSet(resourceName, "id"),
				),
			},
			{
				ResourceName:      resourceName,
				ImportState:       true,
				ImportStateVerify: true,
			},
		},
	})
}

func testAccOkta{{.TitleName}}Config_basic() string {
	return `
resource "okta_{{.TFName}}" "test" {
{{- range .ParentParams}}
  # {{.TFAttr}} = "<{{.TFAttr}}>"
{{- end}}
	// TODO: Add required attributes
}
`
}

func testAccCheckOkta{{.TitleName}}Exists(resourceName string) resource.TestCheckFunc {
	return func(s *terraform.State) error {
		rs, ok := s.RootModule().Resources[resourceName]
		if !ok {
			return fmt.Errorf("resource not found: %s", resourceName)
		}
		if rs.Primary.ID == "" {
			return fmt.Errorf("resource ID is not set")
		}

		// TODO: Implement API call to verify resource exists
		// client := testAccProvider.Meta().(*config.Config).OktaIDaaSClient.OktaSDKClientV6()
{{- if .HasParent}}
		// Retrieve parent param(s) from state attributes:
{{- range .ParentParams}}
		// {{.TFAttr}} := rs.Primary.Attributes["{{.TFAttr}}"]
{{- end}}
		// _, _, err := client.{{.APITag}}API.{{.ReadMethod}}(context.Background(), {{range .ParentParams}}{{.TFAttr}}, {{end}}rs.Primary.ID).Execute()
{{- else}}
		// _, _, err := client.{{.APITag}}API.{{.ReadMethod}}(context.Background(), rs.Primary.ID).Execute()
{{- end}}
		// if err != nil {
		//     return fmt.Errorf("error getting {{.TFName}}: %v", err)
		// }

		return nil
	}
}

func testAccCheckOkta{{.TitleName}}Destroy(s *terraform.State) error {
	for _, rs := range s.RootModule().Resources {
		if rs.Type != "okta_{{.TFName}}" {
			continue
		}

		// TODO: Implement API call to verify resource is destroyed
		// client := testAccProvider.Meta().(*config.Config).OktaIDaaSClient.OktaSDKClientV6()
{{- if .HasParent}}
		// Retrieve parent param(s) from state attributes:
{{- range .ParentParams}}
		// {{.TFAttr}} := rs.Primary.Attributes["{{.TFAttr}}"]
{{- end}}
		// _, httpResp, err := client.{{.APITag}}API.{{.ReadMethod}}(context.Background(), {{range .ParentParams}}{{.TFAttr}}, {{end}}rs.Primary.ID).Execute()
{{- else}}
		// _, httpResp, err := client.{{.APITag}}API.{{.ReadMethod}}(context.Background(), rs.Primary.ID).Execute()
{{- end}}
		// if err == nil {
		//     return fmt.Errorf("{{.TFName}} still exists: %s", rs.Primary.ID)
		// }
		// if httpResp != nil && httpResp.StatusCode != 404 {
		//     return fmt.Errorf("error checking {{.TFName}} destruction: %v", err)
		// }
	}
	return nil
}

// Ensure context is used
var _ = context.Background
